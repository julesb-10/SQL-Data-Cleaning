# SQL-Data-Cleaning
Doing some basic cleaning on a dataset using SQL. Dealing with duplicates without primary key, standardizing data, dealing with NULLs (using a self-join in this case), and removing rows or columns that don't provide any useful information
